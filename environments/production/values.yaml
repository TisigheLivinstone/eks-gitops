replicaCount: 3

image:
  repository: YOUR_ACCOUNT_ID.dkr.ecr.eu-west-1.amazonaws.com/api
  # Image Updater keeps this in sync automatically after each CodeBuild run
  # Never use 'latest' — the SHA makes every deployment traceable to a specific commit
  tag: 5b9336802633203b23cf2db06c87781013647ac2

resources:
  requests:
    cpu: "250m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilization: 70
